Submit four things. Do not submit the scripts or the data files — I wrote those and I already have them. What I want is what you established.

1. A data audit.

data/README.md in the course materials makes specific, checkable claims about dff.csv. Check each one against output you actually ran, and report the result as a table:

Claim in data/README.md	What your executed output showed	Agree?
Series is DFF, Federal Funds Effective Rate		
Units: percent, not seasonally adjusted		
Frequency: daily, 7-day — a value on every calendar day		
Date range		
Row count		
Missing values		
For each row, the middle column must be traceable to something Claude printed. If a claim and the output disagree, say which, by how much, and what you would do about it before using the data.

2. Your prompts.

The prompt you started with, plus anything else you had to ask for and why. The interesting part is the second half: where did the canned prompt prove insufficient, and what did you have to say to get what you needed?

3. The check that passed, and the one that did not.

When you ran 01c_state_cross_section.py, the date-alignment check came back clean — every state reports through the same month. Report that result, and then report what the missing-value check found in the same file.

Then answer this in two or three sentences: why can a data set pass a date alignment check and still have a hole in it? Be specific about what each check actually examines. Finally, say what you would tell a colleague who had already built a chart from this series without running the second check.

4. One paragraph.

Why a monthly mean and a month-end observation answer different questions, and a business situation where choosing the wrong one would lead somewhere costly.

[answer]
a montly mean answers what the month was like when taken as a whole, while a month-end observation shows where the month ended.